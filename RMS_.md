# iRMS Rule Agent 技术方案

## 1. 项目概述

### 1.1 项目名称

**iRMS Rule Agent：面向 AMR 仓储项目的任务流规则生成与沙箱验证工具**

### 1.2 项目定位

iRMS 面对的不是一个固定仓库，而是多个不同项目场景，例如货架到人、料箱到人、分拣、补货、退货、工作站拣选、机器人充电、异常回流等。每个项目都会涉及大量配置，包括任务类型、任务状态流转、工作站规则、点位/区域规则、机器人能力约束、充电规则、异常分支、优先级规则和任务模板。

这些配置通常依赖开发、实施、产品一起理解方案后手工整理，存在落地慢、容易错、项目经验难复用、规则和文档不一致、上线前缺少快速验证等问题。

本项目目标是将仓储业务场景描述转成 iRMS 可用的任务流配置、规则配置和审查结果，帮助开发人员更快完成场景配置开发。

### 1.3 核心目标

- 将结构化仓储业务场景解析为标准场景模型。
- 自动生成 iRMS 任务状态流转配置。
- 自动生成工作站、机器人、电量、异常、优先级等规则配置。
- 对任务流和规则进行确定性审查。
- 提供规则执行沙箱，验证生成配置是否可以跑通。
- 沉淀可复用的场景模板，减少重复配置开发。

## 2. 业务痛点

### 2.1 配置开发依赖人工理解

不同项目的业务流程差异较大，开发人员需要从方案文档中手动拆解任务状态、流转路径、规则条件和异常分支，容易遗漏细节。

### 2.2 规则配置缺少前置校验

任务状态流转和业务规则通常要到联调或现场阶段才暴露问题，例如状态不可达、异常分支缺失、任务无法完成、规则互斥等。

### 2.3 项目经验难以沉淀

历史项目中的规则配置、异常处理和流程经验散落在文档、配置、代码和个人经验中，难以形成可复用模板。

### 2.4 文档、配置和实际执行不一致

方案文档描述、配置文件内容和实际执行链路之间缺少统一视图，导致新人理解成本高，开发修改风险大。

## 3. MVP 范围

### 3.1 首期场景

MVP 只覆盖一个典型场景：**货架到人拣选任务流**。

标准流程：

1. 创建拣选任务
2. 分配机器人
3. 机器人到达货架点
4. 机器人搬运货架到工作站
5. 工作站扫码确认
6. 人工拣选完成
7. 货架回库
8. 任务完成

异常分支：

- 机器人电量低于阈值时不接新任务。
- 工作站满载时任务进入等待。
- 扫码失败时进入人工处理。
- 任务取消时释放机器人和工作站资源。

### 3.2 首期不做内容

- 不覆盖料箱到人、分拣、播种、MIX Pro、冷链、多楼层等复杂场景。
- 不接真实机器人。
- 不实现真实路径规划和避障。
- 不重写 iRMS 调度内核。
- 不做 WMS、ERP、MES、OMS 对接。
- 不做运维监控和日志诊断。
- 不生成复杂业务代码。
- 不做完整测试管理平台。

## 4. 总体架构

系统分为四层：

1. **交互层**
   - 场景录入
   - 规则结果展示
   - 审查报告展示
   - 沙箱执行结果展示

2. **Agent 编排层**
   - 场景解析 Agent
   - 任务流生成 Agent
   - 规则生成 Agent
   - 规则审查 Agent

3. **规则服务层**
   - 场景模型管理
   - 任务流配置管理
   - 规则配置管理
   - 规则审查服务
   - 沙箱执行服务

4. **数据与模板层**
   - 场景模板库
   - 状态机模板库
   - 规则模板库
   - 机器人能力字典
   - 工作站规则字典
   - 异常处理模板

## 5. 核心模块设计

### 5.1 场景录入模块

用于将业务方案转成半结构化输入，避免完全自由文本导致 Agent 输出不稳定。

输入内容包括：

- 场景类型
- 机器人类型
- 工作站数量
- 标准任务步骤
- 点位类型
- 电量阈值
- 工作站限制
- 异常分支
- 任务优先级规则

输出内容：

- 原始场景描述
- 标准化输入表单
- 待解析场景文本

### 5.2 场景解析模块

负责将业务输入解析为标准场景模型。

解析结果包括：

- 场景名称
- 任务类型
- 机器人类型
- 工作站类型
- 状态节点
- 事件触发器
- 规则条件
- 异常事件
- 人工处理节点

### 5.3 任务流生成模块

负责生成任务状态机和状态流转关系。

输出内容包括：

- 状态列表
- 状态流转边
- 触发事件
- 前置条件
- 后置动作
- 成功终态
- 异常终态

示例状态：

```text
CREATED
ASSIGNED
ROBOT_TO_SHELF
SHELF_TO_STATION
WAITING_STATION
PICKING
RETURN_SHELF
FINISHED
EXCEPTION
CANCELLED
```

### 5.4 规则生成模块

负责生成 iRMS 场景规则配置。

规则类型包括：

- 机器人接单规则
- 电量阈值规则
- 工作站满载规则
- 扫码失败规则
- 任务取消规则
- 异常回流规则
- 优先级规则
- 资源释放规则

规则输出应具备结构化格式，便于后续审查和沙箱执行。

### 5.5 规则审查模块

负责对生成的任务流和规则进行确定性审查。

审查内容包括：

- 状态可达性检查
- 完成态可达性检查
- 无出口状态检查
- 异常分支完整性检查
- 互斥规则检查
- 必要阈值缺失检查
- 阻塞状态超时策略检查

审查结果分为：

- 错误：必须修复，否则不可进入沙箱。
- 风险：可进入沙箱，但需要人工确认。
- 建议：可优化项。

### 5.6 沙箱执行模块

负责在模拟任务上下文中执行生成的规则配置。

输入内容：

- 任务编号
- 机器人电量
- 工作站状态
- 扫码结果
- 任务优先级
- 是否触发取消
- 是否触发异常

输出内容：

- 执行状态轨迹
- 触发规则列表
- 阻塞原因
- 异常原因
- 最终执行结果

## 6. 多 Agent 流程设计

### 6.1 场景解析 Agent

职责：

- 读取结构化业务输入。
- 识别场景对象、任务步骤、业务规则和异常分支。
- 输出标准场景模型。

输入：

- 用户填写的场景模板

输出：

- 场景模型 JSON

### 6.2 任务流生成 Agent

职责：

- 根据场景模型生成任务状态机。
- 建立状态节点和流转关系。
- 标记正常流、异常流和取消流。

输入：

- 场景模型 JSON

输出：

- 状态机配置
- 状态流转说明

### 6.3 规则生成 Agent

职责：

- 根据场景模型和状态流转生成规则配置。
- 将业务约束转成结构化规则。
- 输出机器人、工作站、电量、异常、优先级等规则。

输入：

- 场景模型 JSON
- 状态机配置

输出：

- 规则配置 JSON/YAML

### 6.4 规则审查 Agent

职责：

- 调用确定性审查工具。
- 汇总审查结果。
- 给出错误、风险和建议。
- 对需要人工确认的规则给出说明。

输入：

- 状态机配置
- 规则配置

输出：

- 规则审查报告

## 7. 数据模型设计

### 7.1 场景模型

核心字段：

- `scenarioCode`
- `scenarioName`
- `scenarioType`
- `robotType`
- `stationType`
- `taskSteps`
- `constraints`
- `exceptionBranches`
- `priorityRules`

### 7.2 状态机模型

核心字段：

- `workflowCode`
- `states`
- `transitions`
- `startState`
- `successEndStates`
- `exceptionStates`

### 7.3 规则模型

核心字段：

- `ruleCode`
- `ruleName`
- `ruleType`
- `triggerState`
- `condition`
- `action`
- `priority`
- `enabled`
- `riskTags`

### 7.4 沙箱任务上下文

核心字段：

- `taskCode`
- `currentState`
- `robotBattery`
- `stationStatus`
- `scanResult`
- `taskPriority`
- `cancelRequested`
- `exceptionEvent`

## 8. 规则审查设计

### 8.1 状态可达性检查

检查从开始状态出发，是否可以访问所有必要状态。

### 8.2 完成态可达性检查

检查任务是否至少存在一条路径可以到达成功终态。

### 8.3 无出口状态检查

检查非终态节点是否存在出边，避免任务卡死。

### 8.4 异常分支完整性检查

检查所有声明的异常事件是否都有明确处理路径。

### 8.5 互斥规则检查

通过规则标签和互斥矩阵检查明显冲突，例如同一状态下同时要求占用和释放同一资源。

### 8.6 阻塞策略检查

检查等待类状态是否配置超时、重试或人工处理策略。

## 9. 沙箱执行设计

### 9.1 执行目标

沙箱用于证明生成的规则配置不是静态文本，而是可以在模拟上下文中执行并产生可解释轨迹。

### 9.2 执行流程

1. 用户选择场景配置。
2. 用户填写或选择模拟任务上下文。
3. 系统加载状态机和规则配置。
4. 从开始状态执行。
5. 每一步判断可触发规则。
6. 记录状态变化和触发规则。
7. 输出最终结果。

### 9.3 结果类型

- 成功完成
- 进入等待
- 进入人工处理
- 执行失败
- 规则阻塞
- 状态不可达

## 10. 输入输出示例

### 10.1 场景输入示例

```text
场景：货架到人拣选
机器人：潜伏顶升 AMR
标准流程：创建任务 -> 分配机器人 -> 机器人到货架点 -> 搬运到工作站 -> 扫码确认 -> 拣选完成 -> 货架回库 -> 任务完成
电量规则：机器人电量低于 20% 不接新任务
工作站规则：工作站满载时任务等待
异常规则：扫码失败进入人工处理；任务取消后释放机器人和工作站资源
```

### 10.2 审查报告示例

```text
规则审查结果：

1. 状态流完整：CREATED 到 FINISHED 存在可达路径。
2. 异常分支完整：SCAN_FAILED、STATION_FULL、TASK_CANCELLED 均有处理路径。
3. 风险提示：LOW_BATTERY 规则仅限制新任务分配，不影响已执行任务。
4. 风险提示：STATION_FULL 会进入 WAITING_STATION 状态，需配置最大等待时间。
5. 结论：当前规则可进入沙箱执行。
```

### 10.3 沙箱执行结果示例

```text
模拟任务：TASK-001

CREATED -> ASSIGNED
触发机器人筛选规则：电量 85%，满足接单条件
ASSIGNED -> ROBOT_TO_SHELF
ROBOT_TO_SHELF -> SHELF_TO_STATION
触发工作站规则：工作站空闲，允许进入
SHELF_TO_STATION -> PICKING
PICKING -> RETURN_SHELF
RETURN_SHELF -> FINISHED

执行结果：成功
```

## 11. 页面设计

### 11.1 场景录入页面

用于填写业务场景模板，支持选择场景类型、机器人类型、工作站规则和异常分支。

### 11.2 生成结果页面

展示 Agent 生成的场景模型、状态流转、规则配置和异常分支。

### 11.3 规则审查页面

展示错误、风险和建议，支持人工确认或重新生成。

### 11.4 沙箱执行页面

用于填写模拟任务上下文，执行规则并展示状态轨迹。

### 11.5 模板库页面

用于管理已沉淀的场景模板、规则模板和异常处理模板。

## 12. 项目边界

本项目保持以下边界：

- 只做场景规则生成、审查和沙箱验证。
- 不替代 iRMS。
- 不重写 AMR 调度系统。
- 不接真实机器人。
- 不实现真实路径规划。
- 不做外部系统对接。
- 不做运维监控和日志诊断。
- 不做完整测试平台。
- 不生成复杂业务代码。

## 13. MVP 开发计划

### 13.1 第一阶段：基础模型与输入模板

- 定义货架到人拣选场景模板。
- 定义场景模型、状态机模型、规则模型。
- 完成场景录入和结构化解析。

### 13.2 第二阶段：Agent 生成链路

- 实现场景解析 Agent。
- 实现任务流生成 Agent。
- 实现规则生成 Agent。
- 输出状态机和规则配置。

### 13.3 第三阶段：规则审查

- 实现状态可达性检查。
- 实现完成态可达性检查。
- 实现异常分支完整性检查。
- 实现基础互斥规则检查。

### 13.4 第四阶段：规则执行沙箱

- 实现模拟任务上下文。
- 执行状态流转和规则判断。
- 输出执行轨迹。
- 展示成功、等待、异常、失败等结果。

### 13.5 第五阶段：模板沉淀与展示优化

- 支持保存场景模板。
- 支持复用规则模板。
- 优化审查报告和沙箱轨迹展示。

## 14. 风险与规避

### 14.1 输入过于自由导致生成不稳定

规避方式：第一版只支持半结构化模板输入，不接受任意散文式需求。

### 14.2 大模型审查结果不可靠

规避方式：审查逻辑以确定性算法为主，大模型只负责解释和建议。

### 14.3 范围膨胀

规避方式：MVP 只做货架到人拣选，不扩展复杂场景。

### 14.4 沙箱过度接近真实调度

规避方式：沙箱只验证规则执行链路，不实现真实调度、路径规划和机器人控制。

### 14.5 生成配置无法复用

规避方式：建立统一场景模型、状态机模型和规则模型，保证输出结构稳定。

## 15. 预期价值

- 提升 iRMS 场景规则配置开发效率。
- 降低状态流、异常分支、规则配置错误。
- 在联调前提前发现规则问题。
- 帮助历史项目经验沉淀为模板。
- 降低新人理解 iRMS 场景规则的成本。
- 将业务方案、规则配置、规则审查和执行验证形成闭环。

## 16. 后续扩展方向

- 扩展料箱到人场景。
- 扩展智能分拣场景。
- 扩展补货和退货场景。
- 支持规则版本管理。
- 支持规则审批流程。
- 支持与 iRMS 配置中心对接。
- 支持历史项目模板推荐。
- 支持更复杂的场景沙箱。
